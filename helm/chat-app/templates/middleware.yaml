apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "chat-app.fullname" . }}-strip
  namespace: {{ .Release.Namespace }}
spec:
  stripPrefix:
    prefixes:
      - {{ .Values.ingress.basePath }}